CREATE TABLE EMP(
EMP_NO VARCHAR2(30) NOT NULL,
EMP_NAME VARCHAR2(80) NOT NULL,
SALARY NUMBER NULL,
HIRE_DATE DATE NULL
);

ALTER TABLE EMP
MODIFY EMP_NAME VARCHAR2(100);
DESC EMP;

ALTER TABLE EMP
ADD EMP_NAME2 VARCHAR2(80);
DESC EMP;

ALTER TABLE EMP
RENAME COLUMN EMP_NAME2
TO EMP_NAME3;
DESC EMP;

ALTER TABLE EMP
DROP COLUMN EMP_NAME3;
DESC EMP;

DROP TABLE EMP;
DESC EMP;


CREATE TABLE EMP(
EMP_NO VARCHAR2(30) PRIMARY KEY,
EMP_NAME VARCHAR2(80) NOT NULL,
SALARY NUMBER NULL,
HIRE_DATE DATE NULL
);
DESC EMP;

CREATE TABLE EMP2(
EMP_NO VARCHAR2(30),
EMP_NAME VARCHAR2(80) NOT NULL,
SALARY NUMBER NULL,
HIRE_DATE DATE NULL,
PRIMARY KEY(EMP_NO)
);
DESC EMP2;

CREATE TABLE EMP3(
EMP_NO VARCHAR2(30),
EMP_NAME VARCHAR2(80) NOT NULL,
SALARY NUMBER NULL,
HIRE_DATE DATE NULL
);

ALTER TABLE EMP3
ADD CONSTRAINT EMP_3_PK PRIMARY KEY(EMP_NO);

ALTER USER HR IDENTIFIED BY HR ACCOUNT UNLOCK;














